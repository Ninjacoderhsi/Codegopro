package org.ninjacoder.codego

import android.io
import java.12->UTILE
import conext.android
import main.fun
import Timer

    android package new app ()
    {
     start app new app = + funmain()
      this.app(Time)->
      this.liner new li ()
      li = warp 100%
      li = math 100%
      this.imageview new img()
      img = 240dp
      img = 240dp
      
      Timer jjj new Timer()
      Start page main.fun
      #endclass
      }